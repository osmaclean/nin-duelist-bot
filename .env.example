DISCORD_TOKEN=           # Bot token (obrigatório)
DISCORD_CLIENT_ID=       # Application ID (obrigatório)
DISCORD_GUILD_ID=        # Para dev (guild-scoped commands, opcional)
DATABASE_URL=            # Supabase connection string (obrigatório)
ADMIN_ROLE_IDS=          # IDs de cargos admin separados por vírgula (opcional)